# Generate: python -c "import base64,secrets; print(base64.b64encode(secrets.token_bytes(32)).decode())"
SEIF_MASTER_KEY=
# Set a random API key for the demo system when SEIF_DEMO=0.
SEIF_DEMO_API_KEY=
SEIF_DEMO=0
SEIF_TTL_SECONDS=900
# Optional, only with permission to retain original texts. Disabled by default.
# SEIF_REQUEST_CAPTURE_DIR=local-data/hackathon-requests/live-session
# SEIF_REQUEST_CAPTURE_MAX_QUEUED_BYTES=67108864
# SEIF_REQUEST_CAPTURE_MAX_RECORDS=8192
# SEIF_REQUEST_CAPTURE_MAX_DISK_BYTES=2147483648
