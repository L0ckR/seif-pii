# Final code quality evidence — 2384f86

This is a new immutable evidence set. The earlier `benchmarks/code-quality-integration` archive remains unchanged. Machine summary: `docs/code-quality-final.json`.

The final all-source Sonar scan reports zero issues and an OK quality gate. Complete native Ruff reports remain visible: project 0, maintainability 0, raw security 1,020, including 994 pytest assertions. The reviewed CI policy and isolated raw security diagnostics are separate measurements; no findings were removed from these files. Sonar 0% coverage means no coverage report was imported, not that tests were unexecuted. Full pytest verification passed 2,641 tests, no failures/errors/skips.

`golden` contains the actual final rerun and hash/offset proof: all 892 case/profile predictions and full metric profiles equal 1cde5ce. Text-bearing prediction files are excluded. `source-parity.json` proves ten detector/config/vault/transform modules byte-identical to 1cde5ce and Presidio comparison quality functions unchanged; only its benchmark function changed. Consequently the three earlier corpus parity reports are inherited explicitly in `inherited-corpus-evidence.json`; **PII-Bench, RedMadRobot and Scanpatch were not rerun for this final revision**.

`performance/adapter` preserves the original prototype measurements and README verbatim. Their statements that the adapter was not yet installed describe that earlier stage. `production-validation.json`, final source hashes, full tests and isolated NER-layout smoke subsequently verify the installed adapter. Its executable function AST matches the timed prototype, ignoring the docstring. The completed-Future adapter runs bounded response construction on the framework's event-loop thread, with no threadpool handoff or artificial scheduling. It costs one Future allocation and approximately 0.374 microseconds per affected callback; successful /process and model handlers remain ordinary async. The two prototype /process ASGI comparisons measured +0.16% and -1.71%; these short, noisy sequential local measurements are not HTTP capacity/RPS evidence or a zero-overhead claim.

`performance/rejected-sync` is explicitly historical evidence for the rejected plain synchronous callback implementation. Its threadpool latency penalty does not describe the final adapter. It was not selected for the final revision.

`packaging` records the analyzed source ZIP SHA256, matching Git/source hashes, Dockerfile.ner helper inclusion, and a Python 3.13 isolated-copy smoke test with stub NLP and forbidden handler threadpool dispatch. This smoke validates image-layout imports and response contracts; it does not claim a Docker image build or actual model inference. The analyzed source ZIP predates these evidence documents, so a later documentation-inclusive ZIP will have a different checksum.

All copied reports retain original bytes. Gzip files decompress to the original JUnit bytes. `artifact-sha256.json` records content hashes and provenance. No corpus texts, replacement originals, credentials or scanner logs were copied. The scripts retain local reproduction paths and require their documented environments. No deployment, capacity increase or official hackathon score is implied.
