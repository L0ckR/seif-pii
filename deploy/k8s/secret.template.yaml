# Reference schema only. Not included in Kustomize; do not apply this file.
# Generate actual values outside the repository with create-secrets.py.
# Required keys: master-key (base64 of 32 random bytes), api-key, redis-password.
apiVersion: v1
kind: Secret
metadata:
  name: seif-secrets
  namespace: seif
type: Opaque
data: {}
